#include<stdio.h>
const int mx = 100000;
int main(){
    int a, b, c;
    scanf("%d%d%d", &a, &b, &c);

    // Solve the equation below
    // d + e + f = a
    // de + df + ef = b
    // def = c
    // d <= e <= f

    
}